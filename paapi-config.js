import { Configuration } from 'amazon-paapi';

const config = new Configuration({
  accessKey: '',
  secretKey: '',
  partnerTag: 'bookfinderi0d-20',
  region: 'ca-central-1',
});

export default config;
